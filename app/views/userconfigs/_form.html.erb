<%= form_with(model: userconfig, local: true) do |form| %>
  <% if userconfig.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(userconfig.errors.count, "error") %> prohibited this userconfig from being saved:</h2>

      <ul>
        <% userconfig.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :apellidop %>
    <%= form.text_field :apellidop %>
  </div>

  <div class="field">
    <%= form.label :apellidom %>
    <%= form.text_field :apellidom %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_area :descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
