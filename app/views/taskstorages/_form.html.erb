<%= form_with(model: taskstorage, local: true) do |form| %>
  <% if taskstorage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(taskstorage.errors.count, "error") %> prohibited this taskstorage from being saved:</h2>

      <ul>
        <% taskstorage.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="field">
    <%= form.label :sub_title %>
    <%= form.text_field :sub_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
